"use client"

// 'use client' boundary — owns all interactive state.
// Full implementation in frontend spec (08-frontend.md).

export default function ChatClient() {
  return (
    <div className="flex items-center justify-center h-full text-muted">
      Ze — coming soon
    </div>
  )
}
