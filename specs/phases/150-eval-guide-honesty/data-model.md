# Data Model: Eval chat response (remaining)

No tables.

`EvalChatResponse` after this phase: existing fields minus `memory_proposals_count`. Remember/forget judged from `tool_calls`.

Guide indexes: `AGENTS.md` / `CLAUDE.md` phase status rows 140–150 agree with each other and with `specs/README.md`.
